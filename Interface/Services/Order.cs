
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading;
using System.Threading.Tasks;

namespace Interface.Services
{
    public class Order
    {
        //private readonly IT_SYS_LOGRepository oT_SYS_LOGRepository = new T_SYS_LOGRepository();
        //public async Task refOrder(string Params)
        //{
        //    await Task.Delay(60000);
        //    var oLog = oT_SYS_LOGRepository.IQueryable(p => p.Params == Params).First();
        //    oLog.Msg = oLog.Msg + "60s后修改订单" + DateTime.Now;
        //    await oT_SYS_LOGRepository.UpdateAsync(oLog);
        //}
    }
}
